# Пошаговый гайд: Тестирование Android приложения

**Для новичков**  
**Дата**: 2025-01-27

Этот документ содержит пошаговую инструкцию по тестированию Android приложения от установки необходимых программ до успешного запуска и отладки ошибок.

---

## ✅ Хорошие новости: Android Studio уже включает Java!

**Android Studio поставляется со встроенной JDK (JetBrains Runtime)**, поэтому вам **не нужно** устанавливать Java отдельно. Встроенная JDK автоматически используется для сборки и запуска приложений.

**Проверьте версию встроенной Java**:
1. Откройте Android Studio
2. Help → About
3. Посмотрите на строку "Runtime version" — должна быть Java 17 или новее

---

## Содержание

1. [Установка и проверка необходимых программ](#1-установка-и-проверка-необходимых-программ)
2. [Настройка Android Studio](#2-настройка-android-studio)
3. [Настройка эмулятора или подключение устройства](#3-настройка-эмулятора-или-подключение-устройства)
4. [Запуск Unit тестов](#4-запуск-unit-тестов)
5. [Запуск UI тестов](#5-запуск-ui-тестов)
6. [Отладка ошибок](#6-отладка-ошибок)
7. [Скриншоты при ошибках](#7-скриншоты-при-ошибках)
8. [Часто задаваемые вопросы](#8-часто-задаваемые-вопросы)

---

## 1. Установка и проверка необходимых программ

### 1.1 Проверка встроенной Java в Android Studio

**Android Studio включает встроенную JDK (JetBrains Runtime - JBR)**, которая автоматически используется для сборки проектов. Вам **не нужно** устанавливать Java отдельно!

**Проверка версии встроенной Java**:

1. Откройте Android Studio
2. Help → About
3. Посмотрите на строку **"Runtime version"**

**Пример** (из вашего Android Studio):
```
Runtime version: 21.0.7+-13880790-b1038.58 amd64
VM: OpenJDK 64-Bit Server VM by JetBrains s.r.o.
```

✅ **Отлично!** У вас уже установлена Java 21. Android Studio может использовать Java 21 для своей работы.

**Важно для этого проекта**: Проект настроен на **Java 17**, поэтому Gradle должен использовать Java 17, а не Java 21.

**Проверка через Terminal в Android Studio**:

1. В Android Studio откройте вкладку "Terminal" (внизу экрана)
2. Выполните:
   ```powershell
   java -version
   ```
3. Это покажет версию Java, которую использует Gradle (должна быть Java 17 для этого проекта)

**Настройка Gradle для использования Java 17**:

1. File → Settings (или `Ctrl+Alt+S`)
2. Build, Execution, Deployment → Build Tools → Gradle
3. В разделе **"Gradle JDK"** выберите: **`jbr-17`** (JetBrains Runtime 17)
   - ⚠️ **Важно**: Выберите именно `jbr-17`, а не `jbr-21`
   - Если `jbr-17` нет в списке, нажмите "Download JDK..." и выберите версию 17
4. Нажмите OK
5. Синхронизируйте проект: File → Sync Project with Gradle Files

> 💡 **Важно**: 
> - Проект настроен на Java 17 (`compileOptions` и `kotlinOptions` используют Java 17)
> - Gradle должен использовать Java 17, чтобы избежать ошибок "Unsupported class file major version 65"
> - Android Studio может использовать Java 21 для своей работы, но Gradle JDK должен быть Java 17

### 1.2 Установка Android Studio

**Что нужно**: Android Studio Hedgehog (2023.1.1) или новее

**Шаги**:

1. Скачайте Android Studio:
   - Перейдите на [developer.android.com/studio](https://developer.android.com/studio)
   - Скачайте установщик для Windows

2. Запустите установщик и следуйте инструкциям:
   - Выберите стандартную установку (Standard)
   - Установите Android SDK, Android SDK Platform, Android Virtual Device

3. После установки запустите Android Studio

4. При первом запуске:
   - Android Studio предложит скачать дополнительные компоненты
   - Дождитесь завершения загрузки (может занять 10-30 минут)

### 1.3 Проверка Android SDK

**Проверка через Android Studio**:

1. Откройте Android Studio
2. File → Settings (или `Ctrl+Alt+S`)
3. Appearance & Behavior → System Settings → Android SDK
4. Убедитесь, что установлены:
   - **Android SDK Platform 26** (Android 8.0) или новее
   - **Android SDK Build-Tools**
   - **Android Emulator**
   - **Android SDK Platform-Tools**

**Проверка через командную строку**:

```powershell
# Проверка наличия adb (Android Debug Bridge)
adb version
```

**Ожидаемый результат**: версия adb (например, "Android Debug Bridge version 1.0.41")

**Если adb не найден**:

1. Найдите путь к Android SDK (обычно `C:\Users\<ВашеИмя>\AppData\Local\Android\Sdk`)
2. Добавьте в PATH:
   - `C:\Users\<ВашеИмя>\AppData\Local\Android\Sdk\platform-tools`
   - `C:\Users\<ВашеИмя>\AppData\Local\Android\Sdk\tools`

### 1.4 Проверка Git

**Проверка**:
```powershell
git --version
```

**Если Git не установлен**:
- Скачайте с [git-scm.com](https://git-scm.com/download/win)
- Установите с настройками по умолчанию

---

## 2. Настройка Android Studio

### 2.1 Открытие проекта

1. Запустите Android Studio
2. File → Open
3. Выберите папку `c:\work\EastclinicApp`
4. Нажмите OK

### 2.2 Синхронизация Gradle

**Автоматическая синхронизация**:
- Android Studio автоматически начнет синхронизацию Gradle при открытии проекта
- Дождитесь завершения (может занять 5-15 минут при первом запуске)

**Ручная синхронизация**:
- File → Sync Project with Gradle Files
- Или нажмите кнопку "Sync Now" в уведомлении вверху экрана

**Индикаторы синхронизации**:
- В правом нижнем углу Android Studio будет индикатор прогресса
- После завершения появится уведомление "Gradle sync finished"

### 2.2.1 Проверка версии встроенной Java в Android Studio

**Android Studio использует встроенную JDK (JetBrains Runtime)**. Для этого проекта нужно использовать **Java 17**, так как проект настроен на Java 17.

**Проверка через Help → About** (самый простой способ):

1. Help → About
2. Посмотрите на строку **"Runtime version"**:
   - Это версия Java, которую использует сам Android Studio
   - Может быть Java 21, но это нормально

**Проверка через настройки Gradle** (важно для сборки проекта):

1. File → Settings (или `Ctrl+Alt+S`)
2. Build, Execution, Deployment → Build Tools → Gradle
3. В разделе **"Gradle JDK"** проверьте выбранную версию:
   - ✅ **Должно быть**: `jbr-17` (JetBrains Runtime 17) — **рекомендуется для этого проекта**
   - ⚠️ **Может быть**: `jbr-21`, но это может вызвать проблемы (см. ошибку "Unsupported class file major version 65")
   - ❌ **Не должно быть**: системная Java 8

**Если нужно изменить Gradle JDK**:

1. В разделе "Gradle JDK" нажмите на выпадающий список
2. Выберите: **`jbr-17`** (рекомендуется для этого проекта)
   - Если `jbr-17` нет в списке, нажмите "Download JDK..." и выберите версию 17
3. Нажмите OK
4. Синхронизируйте проект: File → Sync Project with Gradle Files

**Проверка через Terminal в Android Studio**:

1. Внизу Android Studio откройте вкладку "Terminal"
2. Выполните:
   ```powershell
   java -version
   ```
3. Должна показаться версия Java 17 (если Gradle JDK настроен на `jbr-17`)

> 💡 **Важно**: 
> - Проект настроен на Java 17 (`compileOptions` и `kotlinOptions` используют Java 17)
> - Gradle должен использовать ту же версию Java, что и проект (Java 17)
> - Если Android Studio использует Java 21, это нормально, но Gradle JDK должен быть Java 17

### 2.3 Проверка сборки проекта

**Через Android Studio**:
1. Build → Make Project (или `Ctrl+F9`)
2. Дождитесь завершения сборки
3. Проверьте панель "Build" внизу экрана на наличие ошибок

**Через командную строку**:
```powershell
# Перейдите в папку проекта
cd c:\work\EastclinicApp

# Запустите сборку
.\gradlew.bat build
```

**Ожидаемый результат**: `BUILD SUCCESSFUL`

**Если сборка не удалась**:
- См. раздел [6. Отладка ошибок](#6-отладка-ошибок)

---

## 3. Настройка эмулятора или подключение устройства

### 3.1 Вариант A: Использование эмулятора (рекомендуется для тестирования)

#### Создание виртуального устройства (AVD)

1. В Android Studio: Tools → Device Manager
2. Нажмите "Create Device"
3. Выберите устройство:
   - Рекомендуется: **Pixel 5** или **Pixel 6**
   - Нажмите "Next"
4. Выберите системный образ:
   - Рекомендуется: **API 33 (Android 13)** или новее
   - Если образа нет, нажмите "Download" рядом с нужным API
   - Дождитесь загрузки (может занять 5-10 минут)
   - Нажмите "Next"
5. Настройте AVD:
   - Имя: например, "Pixel_5_API_33"
   - Оставьте настройки по умолчанию
   - Нажмите "Finish"

#### Запуск эмулятора

**Через Android Studio**:
1. Tools → Device Manager
2. Найдите созданное устройство
3. Нажмите кнопку "Play" (▶) рядом с устройством
4. Дождитесь загрузки эмулятора (может занять 1-2 минуты)

**Через командную строку**:
```powershell
# Список доступных эмуляторов
emulator -list-avds

# Запуск эмулятора (замените Pixel_5_API_33 на имя вашего AVD)
emulator -avd Pixel_5_API_33
```

**Проверка, что эмулятор запущен**:
```powershell
adb devices
```

**Ожидаемый результат**:
```
List of devices attached
emulator-5554    device
```

### 3.2 Вариант B: Подключение реального устройства

#### Включение режима разработчика

1. На Android устройстве: Settings → About phone
2. Найдите "Build number" (или "Номер сборки")
3. Нажмите на "Build number" **7 раз**
4. Появится сообщение "You are now a developer!"

#### Включение USB отладки

1. На Android устройстве: Settings → System → Developer options
2. Включите "USB debugging" (или "Отладка по USB")
3. Подключите устройство к компьютеру через USB

#### Разрешение отладки

1. На устройстве появится запрос "Allow USB debugging?"
2. Поставьте галочку "Always allow from this computer"
3. Нажмите "OK"

#### Проверка подключения

```powershell
adb devices
```

**Ожидаемый результат**:
```
List of devices attached
<серийный_номер>    device
```

**Если устройство не определяется**:
- Убедитесь, что USB отладка включена
- Попробуйте другой USB кабель
- Установите драйверы для вашего устройства (обычно автоматически через Windows Update)

---

## 4. Запуск Unit тестов

### 4.1 Что такое Unit тесты

Unit тесты проверяют отдельные компоненты приложения изолированно (без запуска приложения на устройстве).

**Преимущества**:
- Быстрые (запускаются за секунды)
- Не требуют эмулятор/устройство
- Можно запускать часто во время разработки

### 4.2 Запуск всех Unit тестов

**Через Android Studio**:

1. Откройте проект
2. В левой панели (Project) найдите папку с тестами:
   - Обычно: `app/src/test/java/...` или `core/common/src/test/java/...`
3. Правой кнопкой на папке `test` → Run 'Tests in ...'
4. Результаты появятся в панели "Run" внизу экрана

**Через командную строку**:
```powershell
# Все unit тесты
.\gradlew.bat test

# С подробным выводом
.\gradlew.bat test --info
```

**Ожидаемый результат**:
```
BUILD SUCCESSFUL in 10s
```

### 4.3 Запуск тестов конкретного модуля

**Через командную строку**:
```powershell
# Тесты модуля core:common
.\gradlew.bat :core:common:test

# Тесты модуля feature:auth:presentation
.\gradlew.bat :feature:auth:presentation:test

# Тесты модуля app
.\gradlew.bat :app:test
```

**Через Android Studio**:
1. Найдите файл теста (например, `ResultTest.kt`)
2. Правой кнопкой на файле → Run 'ResultTest'
3. Или нажмите зеленую стрелку слева от класса/метода теста

### 4.4 Просмотр результатов тестов

**В Android Studio**:
- Панель "Run" внизу экрана показывает:
  - ✅ Зеленый кружок = тест прошел
  - ❌ Красный кружок = тест упал
  - Время выполнения каждого теста

**В командной строке**:
- После выполнения команды `.\gradlew.bat test` результаты выводятся в консоль
- Для подробного отчета:
  ```powershell
  .\gradlew.bat test --info > test-results.txt
  ```

### 4.5 Отчеты о тестах

После запуска тестов отчеты сохраняются в:
```
app/build/reports/tests/test/index.html
```

**Просмотр отчета**:
1. Откройте файл `index.html` в браузере
2. Увидите подробную информацию о всех тестах:
   - Какие тесты прошли
   - Какие упали
   - Время выполнения
   - Стек ошибок для упавших тестов

---

## 5. Запуск UI тестов

### 5.1 Что такое UI тесты

UI тесты проверяют интерфейс приложения на реальном устройстве или эмуляторе.

**Требования**:
- ⚠️ **Обязательно**: Запущенный эмулятор или подключенное устройство
- Приложение должно быть собрано и установлено на устройство

### 5.2 Подготовка к UI тестам

**1. Убедитесь, что эмулятор/устройство запущено**:
```powershell
adb devices
```

**Должно показать**:
```
List of devices attached
emulator-5554    device
```

**2. Соберите и установите приложение**:
```powershell
# Сборка и установка debug версии
.\gradlew.bat :app:installDebug
```

**Или через Android Studio**:
1. Выберите конфигурацию запуска "app"
2. Выберите устройство вверху экрана
3. Нажмите Run (▶) или `Shift+F10`

### 5.3 Запуск UI тестов

**Через командную строку**:
```powershell
# Все UI тесты
.\gradlew.bat :app:connectedAndroidTest

# UI тесты конкретного класса
.\gradlew.bat :app:connectedAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.eastclinic.app.navigation.NavigationSmokeTest
```

**Через Android Studio**:

1. Найдите UI тесты:
   - Обычно в `app/src/androidTest/java/...`
2. Правой кнопкой на файле теста → Run 'NavigationSmokeTest'
3. Android Studio автоматически:
   - Соберет приложение
   - Установит на устройство
   - Запустит тесты
   - Покажет результаты

### 5.4 Просмотр результатов UI тестов

**В Android Studio**:
- Панель "Run" показывает результаты
- Можно видеть скриншоты, если тест упал (см. раздел [7. Скриншоты при ошибках](#7-скриншоты-при-ошибках))

**Отчеты**:
```
app/build/reports/androidTests/connected/index.html
```

Откройте файл в браузере для подробного отчета.

### 5.5 Проблемы с UI тестами

**Тесты не запускаются**:
- Убедитесь, что устройство подключено: `adb devices`
- Убедитесь, что приложение установлено: `adb shell pm list packages | grep eastclinic`

**Тесты падают из-за таймаутов**:
- Увеличьте время ожидания в тестах
- Убедитесь, что эмулятор не тормозит (закройте другие приложения)

**Тесты не находят элементы UI**:
- Убедитесь, что используются `testTag` в Compose компонентах
- Проверьте, что экран успел загрузиться перед проверкой

---

## 6. Отладка ошибок

### 6.1 Типичные ошибки сборки

#### Ошибка: "Gradle sync failed"

**Причины**:
- Проблемы с интернет-соединением (Gradle скачивает зависимости)
- Неправильная версия JDK
- Поврежденный кеш Gradle

**Решение**:
```powershell
# 1. Очистка кеша Gradle
.\gradlew.bat clean

# 2. Очистка кеша Android Studio
# В Android Studio: File → Invalidate Caches → Invalidate and Restart

# 3. Удаление папки .gradle (в папке проекта)
# Удалите папку .gradle и пересоберите проект
```

#### Ошибка: "SDK location not found"

**Решение**:
1. File → Settings → Appearance & Behavior → System Settings → Android SDK
2. Скопируйте путь "Android SDK Location"
3. Создайте файл `local.properties` в корне проекта:
   ```properties
   sdk.dir=C:\\Users\\<ВашеИмя>\\AppData\\Local\\Android\\Sdk
   ```
4. Замените `<ВашеИмя>` на ваше имя пользователя Windows

#### Ошибка: "Incompatible Gradle JVM" / "Your build is currently configured to use incompatible Java 21.0.7 and Gradle 8.0"

**Причина**: Gradle 8.0 не поддерживает Java 21. Gradle 8.0 поддерживает максимум Java 19.

**Пример ошибки**:
```
Your build is currently configured to use incompatible Java 21.0.7 and Gradle 8.0.
Cannot sync the project.

The minimum compatible Gradle version is 8.5.
The maximum compatible Gradle JVM version is 19.
```

**Решение**: Обновите версию Gradle до 8.5 или выше (рекомендуется 8.10).

**Шаг 1: Обновите версию Gradle в проекте**

Откройте файл `gradle/wrapper/gradle-wrapper.properties` и измените:
```properties
# Было:
distributionUrl=https\://services.gradle.org/distributions/gradle-8.0-bin.zip

# Должно быть:
distributionUrl=https\://services.gradle.org/distributions/gradle-8.10-bin.zip
```

Также обновите версию в `gradle/libs.versions.toml`:
```toml
# Было:
gradle = "8.0"

# Должно быть:
gradle = "8.10"
```

**Шаг 2: Синхронизируйте проект**

1. В Android Studio: File → Sync Project with Gradle Files
2. Или через командную строку:
   ```powershell
   .\gradlew.bat --stop
   .\gradlew.bat build
   ```

**Проверка после исправления**:
- Проект должен синхронизироваться без ошибок
- В панели "Build" не должно быть ошибок про несовместимость Java и Gradle

> 💡 **Важно**: Gradle 8.5+ поддерживает Java 21. Если у вас Android Studio с Java 21, используйте Gradle 8.5 или новее.

#### Ошибка: "Unsupported class file major version 65"

**Причина**: Gradle использует Java 21 (major version 65), но Android Gradle Plugin 8.1.4 или некоторые зависимости не полностью поддерживают Java 21. Проект настроен на Java 17.

**Пример ошибки**:
```
java.lang.IllegalArgumentException: Unsupported class file major version 65
```

> **Справочник версий**:
> - Версия 65 = Java 21
> - Версия 61 = Java 17
> - Версия 52 = Java 8

**Решение**: Используйте Java 17 для Gradle (проект уже настроен на Java 17).

**Шаг 1: Настройте Gradle JDK на Java 17**

1. File → Settings (или `Ctrl+Alt+S`)
2. Build, Execution, Deployment → Build Tools → Gradle
3. В разделе **"Gradle JDK"** выберите: **`jbr-17`** (JetBrains Runtime 17)
   - ⚠️ **Важно**: Выберите именно `jbr-17`, а не `jbr-21`
4. Нажмите OK

**Шаг 2: Очистите кэш и пересоберите**

```powershell
# Остановите Gradle daemon
.\gradlew.bat --stop

# Очистите проект
.\gradlew.bat clean
```

**Шаг 3: Синхронизируйте проект**

1. File → Sync Project with Gradle Files
2. Или через командную строку:
   ```powershell
   .\gradlew.bat build
   ```

**Проверка после исправления**:
- В Android Studio Terminal: `java -version` — должна быть версия 17 (не 21)
- Проект должен собираться без ошибок

> 💡 **Важно**: Проект настроен на Java 17 (`compileOptions` и `kotlinOptions` используют Java 17). Gradle должен использовать ту же версию Java, что и проект.

#### Ошибка: "AGPBI: Unsupported class file major version 65" в D8

**Причина**: JVM модули (domain модули и core:common) были скомпилированы с Java 21, но D8 (Android dex compiler) не может обработать классы, скомпилированные с Java 21. Нужно компилировать с Java 17.

**Пример ошибки**:
```
AGPBI: {"kind":"error","text":"java.lang.IllegalArgumentException: Unsupported class file major version 65","sources":[{"file":"...\\feature\\auth\\domain\\build\\classes\\kotlin\\main\\...\\User.class"}],"tool":"D8"}
```

> Версия 65 = Java 21, версия 61 = Java 17

**Решение**: Убедитесь, что все JVM модули компилируются с Java 17.

**Шаг 1: Проверьте настройки Gradle JDK**

1. File → Settings → Build, Execution, Deployment → Build Tools → Gradle
2. В разделе "Gradle JDK" выберите: **`jbr-17`** (не `jbr-21`)
3. Нажмите OK

**Шаг 2: Очистите build папки всех JVM модулей**

```powershell
# Остановите Gradle daemon
.\gradlew.bat --stop

# Очистите build папки JVM модулей (core:common и все domain модули)
Remove-Item -Recurse -Force "core\common\build" -ErrorAction SilentlyContinue
Get-ChildItem -Path "feature" -Directory -Recurse -Filter "domain" | ForEach-Object { 
    Remove-Item -Recurse -Force "$($_.FullName)\build" -ErrorAction SilentlyContinue 
}

# Или очистите весь проект (удалит все build папки)
.\gradlew.bat clean
```

**Шаг 3: Синхронизируйте проект**

1. В Android Studio: File → Sync Project with Gradle Files
2. Дождитесь завершения синхронизации

**Шаг 4: Пересоберите проект**

1. В Android Studio: Build → Make Project (или `Ctrl+F9`)
2. Или через командную строку:
   ```powershell
   .\gradlew.bat build
   ```

**Проверка после исправления**:
- Все модули должны компилироваться с Java 17
- D8 должен успешно обрабатывать скомпилированные классы
- Проект должен собираться без ошибок

> 💡 **Важно**: В корневом `build.gradle.kts` добавлена общая конфигурация для всех JVM модулей, которая гарантирует использование Java 17. Если ошибка останется, убедитесь, что Gradle JDK настроен на Java 17.

#### Ошибка: "Cannot find a Java installation on your machine matching this tasks requirements: {languageVersion=17}"

**Причина**: Gradle не может найти Java 17 для toolchain. Toolchain пытается скачать или найти Java 17, но не может.

**Пример ошибки**:
```
Cannot find a Java installation on your machine matching this tasks requirements: {languageVersion=17, vendor=any vendor, implementation=vendor-specific} for WINDOWS on x86_64.
No locally installed toolchains match and toolchain download repositories have not been configured.
```

**Решение**: Убрать toolchain и использовать Gradle JDK напрямую (который настроен в Android Studio).

В корневом `build.gradle.kts` конфигурация должна быть без toolchain:
```kotlin
subprojects {
    plugins.withId("org.jetbrains.kotlin.jvm") {
        apply(plugin = "java-library")
        
        // Настройка Kotlin компиляции для Java 17
        tasks.withType<org.jetbrains.kotlin.gradle.tasks.KotlinCompile>().configureEach {
            kotlinOptions {
                jvmTarget = "17"
            }
        }
        
        // Настройка Java компиляции для Java 17 (использует Gradle JDK)
        tasks.withType<JavaCompile>().configureEach {
            sourceCompatibility = "17"
            targetCompatibility = "17"
            options.release.set(17)
        }
    }
}
```

**Важно**: Убедитесь, что Gradle JDK настроен на Java 17 в Android Studio:
- File → Settings → Build, Execution, Deployment → Build Tools → Gradle
- "Gradle JDK" → выберите `jbr-17`

#### Ошибка: "Inconsistent JVM-target compatibility detected for tasks 'compileJava' (21) and 'compileKotlin' (17)"

**Причина**: Несоответствие версий Java между задачами компиляции. `compileJava` использует Java 21, а `compileKotlin` использует Java 17. Оба должны использовать одну версию.

**Пример ошибки**:
```
Inconsistent JVM-target compatibility detected for tasks 'compileJava' (21) and 'compileKotlin' (17).
Consider using JVM Toolchain: https://kotl.in/gradle/jvm/toolchain
```

**Решение**: Настроить Java toolchain для JVM модулей, чтобы обе задачи использовали Java 17.

**Шаг 1: Проверьте настройки Gradle JDK**

1. File → Settings → Build, Execution, Deployment → Build Tools → Gradle
2. В разделе "Gradle JDK" должно быть: **`jbr-17`** (не `jbr-21`)
3. Нажмите OK

**Шаг 2: Убедитесь, что конфигурация в build.gradle.kts правильная**

В корневом `build.gradle.kts` должна быть конфигурация для JVM модулей с Java toolchain. Если её нет, добавьте (уже должно быть добавлено автоматически).

**Шаг 3: Очистите и пересоберите проект**

```powershell
# Остановите Gradle daemon
.\gradlew.bat --stop

# Очистите build папки JVM модулей
Remove-Item -Recurse -Force "core\common\build" -ErrorAction SilentlyContinue
Get-ChildItem -Path "feature" -Directory -Recurse -Filter "domain" | ForEach-Object { 
    Remove-Item -Recurse -Force "$($_.FullName)\build" -ErrorAction SilentlyContinue 
}

# Очистите весь проект
.\gradlew.bat clean
```

**Шаг 4: Синхронизируйте и пересоберите проект**

1. File → Sync Project with Gradle Files
2. Build → Make Project (или `Ctrl+F9`)

**Проверка после исправления**:
- Обе задачи (`compileJava` и `compileKotlin`) должны использовать Java 17
- Проект должен собираться без ошибок

> 💡 **Важно**: Конфигурация в корневом `build.gradle.kts` автоматически применяет Java toolchain для всех JVM модулей. Убедитесь, что Gradle JDK настроен на Java 17.

#### Ошибка: "Unsupported class file major version 61" (или другая версия)

**Причина**: Несоответствие версии Java между Gradle и проектом, или старая версия Java.

**Пример ошибки**:
```
Unsupported class file major version 61
```
> Версия 61 = Java 17, версия 52 = Java 8

**Решение** (используйте встроенную Java из Android Studio):

1. **Проверьте версию встроенной Java в Android Studio**:
   - Help → About → посмотрите "Runtime version"
   - Должна быть Java 17 или новее

2. **Настройте Gradle для использования встроенной Java**:
   - File → Settings → Build, Execution, Deployment → Build Tools → Gradle
   - В разделе "Gradle JDK" выберите: `jbr-17` (рекомендуется для этого проекта)
   - Нажмите OK

3. **Очистите кеш и пересоберите**:
   ```powershell
   .\gradlew.bat clean
   ```

4. **Синхронизируйте проект**: File → Sync Project with Gradle Files

**Проверка после исправления**:
- В Android Studio Terminal: `java -version` — должна быть версия 17+

#### Ошибка: "Could not determine java version from '8'"

**Причина**: Gradle пытается использовать Java 8, но проект требует Java 17+

**Решение**:

1. **Настройте Gradle JDK в Android Studio**:
   - File → Settings → Build, Execution, Deployment → Build Tools → Gradle
   - "Gradle JDK" → выберите `jbr-21` или `jbr-17` (встроенная Java)
   - Нажмите OK

2. **Очистите кеш Gradle**:
   ```powershell
   .\gradlew.bat clean
   ```

3. **Перезапустите Android Studio** (если нужно)

4. **Синхронизируйте проект**: File → Sync Project with Gradle Files

#### Ошибка: "Gradle sync failed: Java 8 is not supported"

**Причина**: Проект явно требует Java 17+, но Gradle использует Java 8

**Решение**:

1. **Используйте встроенную Java из Android Studio**:
   - File → Settings → Build, Execution, Deployment → Build Tools → Gradle
   - "Gradle JDK" → выберите `jbr-21` или `jbr-17` (встроенная JetBrains Runtime)
   - Нажмите OK

2. **Перезапустите Android Studio**

3. **Синхронизируйте проект заново**: File → Sync Project with Gradle Files

> 💡 **Важно**: Android Studio уже включает Java 17+ (JetBrains Runtime). Просто убедитесь, что в настройках Gradle выбрана встроенная версия, а не системная Java 8.

#### Ошибка: "JdkImageTransform" / "jlink.exe finished with non-zero exit value 1" / "Failed to transform core-for-system-modules.jar"

**Причина**: Android Gradle Plugin пытается использовать `jlink.exe` из Java 21 (jbr), но это вызывает проблемы. Это известная проблема с AGP 8.1.4 и Java 21.

**Пример ошибки**:
```
Failed to transform core-for-system-modules.jar to match attributes {artifactType=_internal_android_jdk_image}
Error while executing process ...\jbr\bin\jlink.exe ... finished with non-zero exit value 1
```

**Решение**: **КРИТИЧЕСКИ ВАЖНО** - настроить Gradle JDK на Java 17, затем очистить кэш трансформаций.

**Шаг 1: Убедитесь, что Gradle JDK настроен на Java 17** ⚠️ **ОБЯЗАТЕЛЬНО!**

1. Откройте Android Studio
2. File → Settings (или `Ctrl+Alt+S`)
3. Build, Execution, Deployment → Build Tools → Gradle
4. В разделе **"Gradle JDK"** найдите выпадающий список
   - ⚠️ **ПРОБЛЕМА**: Если у вас выбрано `GRADLE_LOCAL_JAVA_HOME` или путь `C:\Program Files\Android\Android Studio\jbr` — это Java 21, и это вызывает ошибку!
   - **РЕШЕНИЕ**: Выберите из списка **`jbr-17`** (JetBrains Runtime 17)
   - Если `jbr-17` нет в списке:
     - Нажмите на выпадающий список "Gradle JDK"
     - Выберите "Download JDK..." (внизу списка)
     - В открывшемся окне выберите:
       - **Vendor**: JetBrains Runtime (JBR)
       - **Version**: **17** (обязательно 17, не 21!)
       - **Location**: оставьте по умолчанию
     - Нажмите "Download"
     - Дождитесь загрузки и установки (может занять несколько минут)
     - После установки `jbr-17` появится в списке
5. Выберите **`jbr-17`** из списка
6. Нажмите **Apply**, затем **OK**
7. **Перезапустите Android Studio** (критически важно для применения изменений!)

**Проверка настройки после перезапуска**:
- Откройте Settings → Build, Execution, Deployment → Build Tools → Gradle
- Убедитесь, что в "Gradle JDK" выбран **`jbr-17`** (не `GRADLE_LOCAL_JAVA_HOME` и не путь с `jbr`)
- В Terminal Android Studio выполните: `java -version`
  - ✅ **Правильно**: `openjdk version "17.x.x"` или `java version "17.x.x"`
  - ❌ **Неправильно**: `openjdk version "21.x.x"` или `java version "21.x.x"`

**Шаг 2: Остановите Gradle daemon и очистите кэш**

```powershell
# Остановите все Java процессы (Gradle daemon)
Get-Process -Name "java" -ErrorAction SilentlyContinue | Stop-Process -Force -ErrorAction SilentlyContinue
Start-Sleep -Seconds 2

# Очистите весь кэш трансформаций (включая JdkImageTransform)
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\caches\8.10\transforms" -ErrorAction SilentlyContinue

# Очистите кэш Gradle daemon
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\daemon" -ErrorAction SilentlyContinue

# Очистите локальную папку .gradle проекта
cd c:\work\EastclinicApp
if (Test-Path ".gradle") { Remove-Item -Recurse -Force ".gradle" -ErrorAction SilentlyContinue }
```

**Шаг 3: Синхронизируйте проект**

1. В Android Studio: File → Sync Project with Gradle Files
2. Дождитесь завершения синхронизации

**Шаг 4: Пересоберите проект**

1. Build → Make Project (или `Ctrl+F9`)
2. Или через командную строку:
   ```powershell
   .\gradlew.bat build
   ```

**Если ошибка останется**:

1. **Проверьте, что Gradle JDK действительно настроен на `jbr-17`**:
   - File → Settings → Build, Execution, Deployment → Build Tools → Gradle
   - Убедитесь, что выбран именно `jbr-17`, а не `jbr-21` или `jbr`
   - Если `jbr-17` нет в списке, скачайте его через "Download JDK..."
2. **Перезапустите Android Studio** (обязательно после изменения Gradle JDK!)
3. **Проверьте версию Java в Terminal Android Studio**:
   ```powershell
   java -version
   ```
   Должна быть версия 17.x.x, а не 21.x.x
4. Попробуйте обновить Android Gradle Plugin до версии 8.2+ или использовать Gradle 8.9 вместо 8.10

> ⚠️ **КРИТИЧЕСКИ ВАЖНО**: 
> - Эта проблема **всегда** возникает, если Gradle JDK настроен на Java 21
> - **Обязательно** настройте Gradle JDK на `jbr-17` в Android Studio
> - **Обязательно** перезапустите Android Studio после изменения Gradle JDK
> - Проблема связана с тем, что Android Gradle Plugin 8.1.4 не полностью совместим с Java 21 для JdkImageTransform
> - Использование Java 17 для Gradle JDK **обязательно** для этого проекта

#### Ошибка: "Configuration cache state could not be cached" / "JdkImageTransform" (старая версия)

**Причина**: Проблема с Configuration Cache и обработкой JDK-образов в Android Gradle Plugin. Это известная проблема при использовании Configuration Cache с AGP 8.1.4 и Gradle 8.10.

**Решение**: Отключите Configuration Cache (временно, пока проблема не будет исправлена в будущих версиях AGP).

**Шаг 1: Отключите Configuration Cache**

Откройте файл `gradle.properties` и измените:
```properties
# Было:
org.gradle.configuration-cache=true

# Должно быть:
org.gradle.configuration-cache=false
```

**Шаг 2: Очистите кэш Gradle**

```powershell
# Остановите Gradle daemon
.\gradlew.bat --stop

# Очистите кэш проекта
.\gradlew.bat clean

# Очистите кэш Configuration Cache (если есть)
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\caches\8.10\configuration-cache" -ErrorAction SilentlyContinue
```

**Шаг 3: Синхронизируйте проект**

1. В Android Studio: File → Sync Project with Gradle Files
2. Или через командную строку:
   ```powershell
   .\gradlew.bat build
   ```

> 💡 **Примечание**: Configuration Cache — это экспериментальная функция, которая ускоряет сборку, но может вызывать проблемы с некоторыми плагинами. Для стабильной работы рекомендуется отключить её до исправления проблемы в AGP.

#### Ошибка: "FileNotFoundException: aar-metadata.properties" / "NoClassDefFoundError: org/apache/commons/io/FileUtils" после очистки кэша transforms

**Причина**: После удаления папки `transforms` в кэше Gradle, Gradle не может найти трансформированные артефакты (AAR, JAR) и их метаданные. Это происходит, если была удалена только папка `transforms`, но не весь кэш Gradle.

**Пример ошибки**:
```
Execution failed for task ':app:checkDebugAarMetadata'.
> C:\Users\user\.gradle\caches\8.10\transforms\...\navigation-common-2.7.6\META-INF\com\android\build\gradle\aar-metadata.properties (Системе не удается найти указанный путь)

Execution failed for task ':app:writeDebugSigningConfigVersions'.
> java.lang.NoClassDefFoundError: org/apache/commons/io/FileUtils
```

**Решение**: Очистить весь кэш Gradle 8.10, чтобы Gradle пересобрал все зависимости с нуля.

**Шаг 1: Очистите весь кэш Gradle 8.10**

```powershell
# Очистите весь кэш Gradle 8.10 (включая transforms, modules, jars и т.д.)
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\caches\8.10" -ErrorAction SilentlyContinue
```

**Шаг 2: Очистите локальные build папки проекта**

```powershell
# Перейдите в корень проекта
cd c:\work\EastclinicApp

# Очистите build папки
if (Test-Path "build") { Remove-Item -Recurse -Force "build" -ErrorAction SilentlyContinue }
Get-ChildItem -Path "." -Directory -Filter "build" -Recurse | ForEach-Object { 
    Remove-Item -Recurse -Force $_.FullName -ErrorAction SilentlyContinue 
}

# Очистите локальную папку .gradle
if (Test-Path ".gradle") { Remove-Item -Recurse -Force ".gradle" -ErrorAction SilentlyContinue }
```

**Шаг 3: Синхронизируйте проект**

1. В Android Studio: File → Sync Project with Gradle Files
2. Gradle автоматически загрузит и трансформирует все зависимости заново
3. Это может занять несколько минут при первом запуске

**Шаг 4: Пересоберите проект**

1. Build → Make Project (или `Ctrl+F9`)
2. Или через командную строку:
   ```powershell
   .\gradlew.bat build
   ```

> 💡 **Важно**: 
> - Если вы удалили только папку `transforms`, Gradle не сможет найти уже трансформированные артефакты
> - Нужно удалить весь кэш `8.10`, чтобы Gradle пересобрал все зависимости
> - После очистки кэша первая сборка будет дольше, так как Gradle заново загрузит и трансформирует все зависимости

#### Ошибка: "Could not read workspace metadata from kotlin-dsl/accessors/.../metadata.bin" после очистки кэша

**Причина**: После очистки кэша Gradle 8.10, Gradle не может найти метаданные Kotlin DSL accessors. Это происходит, если была удалена папка `kotlin-dsl` в кэше, но Gradle пытается использовать старые метаданные.

**Пример ошибки**:
```
Could not read workspace metadata from C:\Users\user\.gradle\caches\8.10\kotlin-dsl\accessors\cffafdf102105b9e175cadcfcd21d2c3\metadata.bin
> C:\Users\user\.gradle\caches\8.10\kotlin-dsl\accessors\...\metadata.bin (Системе не удается найти указанный путь)
```

**Решение**: Остановить Gradle daemon и очистить кэш Kotlin DSL, чтобы Gradle пересоздал accessors заново.

**Шаг 1: Остановите Gradle daemon**

```powershell
# Остановите все запущенные Gradle daemon процессы
.\gradlew.bat --stop
```

**Шаг 2: Очистите кэш Kotlin DSL и daemon**

```powershell
# Очистите кэш Kotlin DSL accessors
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\caches\8.10\kotlin-dsl" -ErrorAction SilentlyContinue

# Очистите кэш Gradle daemon (опционально, но рекомендуется)
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\daemon" -ErrorAction SilentlyContinue
```

**Шаг 3: Очистите локальную папку .gradle проекта**

```powershell
# Перейдите в корень проекта
cd c:\work\EastclinicApp

# Очистите локальную папку .gradle
if (Test-Path ".gradle") { Remove-Item -Recurse -Force ".gradle" -ErrorAction SilentlyContinue }
```

**Шаг 4: Синхронизируйте проект**

1. В Android Studio: File → Sync Project with Gradle Files
2. Gradle автоматически пересоздаст Kotlin DSL accessors
3. Это может занять несколько минут при первом запуске

**Шаг 5: Пересоберите проект**

1. Build → Make Project (или `Ctrl+F9`)
2. Или через командную строку:
   ```powershell
   .\gradlew.bat build
   ```

> 💡 **Важно**: 
> - После очистки кэша `kotlin-dsl`, Gradle автоматически пересоздаст accessors при следующей синхронизации
> - Если ошибка останется, попробуйте очистить весь кэш Gradle 8.10: `Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\caches\8.10"`

#### Ошибка: "Multiple build operations failed" / "Could not read workspace metadata from transforms/.../metadata.bin" (множественные повреждённые папки)

**Причина**: Несколько папок в `transforms` кэше повреждены или содержат неполные метаданные. Gradle пытается использовать старые метаданные из повреждённых папок, но не может найти файлы `metadata.bin`. Это часто происходит после частичной очистки кэша или прерванной сборки.

**Пример ошибки**:
```
Multiple build operations failed.
    Could not read workspace metadata from C:\Users\user\.gradle\caches\8.10\transforms\3444c3b3a77b96efbbd080774a12cfac\metadata.bin
    Could not read workspace metadata from C:\Users\user\.gradle\caches\8.10\transforms\6cf47c10c23f262c9cd640b55e138949\metadata.bin
    Could not read workspace metadata from C:\Users\user\.gradle\caches\8.10\transforms\7509bd20d8b52570a3a2501b74e901a5\metadata.bin
    Could not read workspace metadata from C:\Users\user\.gradle\caches\8.10\transforms\3a0bcab3b513355ee46676240104779b\metadata.bin
```

**Решение**: Полностью остановить Gradle daemon и очистить всю папку `transforms`, чтобы Gradle пересоздал все трансформации с нуля.

**Шаг 1: Остановите все Gradle процессы**

```powershell
# Остановите Gradle daemon через gradlew (если доступен)
.\gradlew.bat --stop

# Принудительно остановите все Java процессы, связанные с Gradle
Get-Process -Name "java" -ErrorAction SilentlyContinue | 
    Where-Object { $_.Path -like "*gradle*" -or $_.CommandLine -like "*gradle*" } | 
    Stop-Process -Force -ErrorAction SilentlyContinue

# Подождите 2 секунды, чтобы процессы завершились
Start-Sleep -Seconds 2
```

**Шаг 2: Очистите папку transforms и daemon кэш**

```powershell
# Удалите всю папку transforms
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\caches\8.10\transforms" -ErrorAction SilentlyContinue

# Очистите кэш Gradle daemon
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\daemon" -ErrorAction SilentlyContinue
```

**Шаг 3: Очистите локальные папки проекта**

```powershell
# Перейдите в корень проекта
cd c:\work\EastclinicApp

# Очистите локальную папку .gradle
if (Test-Path ".gradle") { 
    Remove-Item -Recurse -Force ".gradle" -ErrorAction SilentlyContinue 
}

# Очистите build папки (опционально)
if (Test-Path "build") { 
    Remove-Item -Recurse -Force "build" -ErrorAction SilentlyContinue 
}
Get-ChildItem -Path "." -Directory -Filter "build" -Recurse | ForEach-Object { 
    Remove-Item -Recurse -Force $_.FullName -ErrorAction SilentlyContinue 
}
```

**Шаг 4: Синхронизируйте проект**

1. В Android Studio: File → Sync Project with Gradle Files
2. Gradle автоматически пересоздаст все трансформации и метаданные
3. Это может занять несколько минут при первом запуске

**Шаг 5: Пересоберите проект**

1. Build → Make Project (или `Ctrl+F9`)
2. Или через командную строку:
   ```powershell
   .\gradlew.bat build
   ```

> 💡 **Важно**: 
> - **Всегда останавливайте Gradle daemon перед очисткой кэша**, чтобы избежать повреждения метаданных
> - Если проблема повторяется, попробуйте очистить весь кэш Gradle 8.10: `Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\caches\8.10"`
> - После очистки кэша первая сборка будет дольше, так как Gradle заново загрузит и трансформирует все зависимости

### 6.2 Ошибки компиляции Kotlin

#### Ошибка: "Inheritance of sealed classes or interfaces from different module is prohibited"

**Причина**: Sealed классы в Kotlin не могут наследоваться из разных модулей. Все наследники sealed класса должны быть в том же модуле, где объявлен sealed класс.

**Пример ошибки**:
```
Inheritance of sealed classes or interfaces from different module is prohibited
Inheritor of sealed class or interface declared in package com.eastclinic.auth.presentation.login 
but it must be in package com.eastclinic.core.ui where base class is declared
```

**Решение**: Изменить sealed class на interface или abstract class, если нужно наследоваться в других модулях.

**Пример**:
```kotlin
// Было (sealed class - нельзя наследоваться в других модулях):
sealed class UiEffect {
    data class Navigate(val route: String) : UiEffect()
}

// Должно быть (interface - можно реализовывать в любом модуле):
interface UiEffect

// В feature модуле:
sealed class LoginUiEffect : UiEffect {
    data class NavigateToHome(val route: String) : LoginUiEffect()
    data class ShowError(val message: String) : LoginUiEffect()
}
```

#### Ошибка: "Unresolved reference: message" в AppError

**Причина**: `AppError` - это sealed class, и нельзя обращаться к свойству `message` напрямую, если компилятор не знает конкретный тип.

**Пример ошибки**:
```kotlin
// Ошибка:
_uiEffect.emit(LoginUiEffect.ShowError(result.error.message))
// Unresolved reference: message
```

**Решение**: Добавить абстрактное свойство `message` в базовый класс `AppError`.

**Пример**:
```kotlin
// В AppError.kt:
sealed class AppError {
    abstract val message: String  // Добавить абстрактное свойство
    
    data class NetworkError(
        override val message: String,  // override
        val code: Int? = null
    ) : AppError()
    
    // ... другие варианты
}
```

Теперь можно обращаться к `result.error.message` напрямую.

### 6.3 Ошибки тестов

#### Тест падает с ошибкой "ClassNotFoundException"

**Причина**: Зависимости не добавлены в `build.gradle.kts`

**Решение**:
1. Проверьте, что в `build.gradle.kts` модуля есть:
   ```kotlin
   dependencies {
       testImplementation(libs.junit4)
       testImplementation(libs.mockk)
       // и другие тестовые зависимости
   }
   ```
2. Синхронизируйте Gradle: File → Sync Project with Gradle Files

#### Тест падает с ошибкой "kotlinx.coroutines.test"

**Причина**: Неправильное использование корутин в тестах

**Решение**:
- Используйте `runTest` из `kotlinx-coroutines-test`:
  ```kotlin
  import kotlinx.coroutines.test.runTest
  
  @Test
  fun myTest() = runTest {
      // ваш тест
  }
  ```

#### Тест падает с ошибкой "MockK"

**Причина**: Неправильная настройка моков

**Решение**:
```kotlin
// Для suspend функций используйте coEvery
coEvery { repository.getData() } returns Result.Success(data)

// Для обычных функций используйте every
every { clock.now() } returns Instant.now()
```

### 6.3 Ошибки при запуске приложения

#### Ошибка: "resource mipmap/ic_launcher not found" / "Android resource linking failed"

**Причина**: В AndroidManifest.xml указаны иконки приложения (`@mipmap/ic_launcher` и `@mipmap/ic_launcher_round`), но соответствующие ресурсы отсутствуют в папке `app/src/main/res/mipmap-*/`.

**Пример ошибки**:
```
Android resource linking failed
error: resource mipmap/ic_launcher (aka com.eastclinic.app:mipmap/ic_launcher) not found.
error: resource mipmap/ic_launcher_round (aka com.eastclinic.app:mipmap/ic_launcher_round) not found.
```

**Решение 1: Использовать стандартные системные иконки (быстрое решение)**

Откройте `app/src/main/AndroidManifest.xml` и измените:
```xml
<!-- Было: -->
android:icon="@mipmap/ic_launcher"
android:roundIcon="@mipmap/ic_launcher_round"

<!-- Должно быть: -->
android:icon="@android:drawable/sym_def_app_icon"
android:roundIcon="@android:drawable/sym_def_app_icon"
```

**Решение 2: Создать собственные иконки (правильное решение)**

1. В Android Studio: File → New → Image Asset
2. Выберите "Launcher Icons (Adaptive and Legacy)"
3. Настройте иконку (можно использовать текст, изображение или иконку)
4. Нажмите "Next" и "Finish"
5. Android Studio автоматически создаст все необходимые ресурсы в папках `mipmap-*`

**Проверка после исправления**:
- Проект должен собираться без ошибок
- Приложение должно устанавливаться и запускаться

> 💡 **Совет**: Для production приложения рекомендуется создать собственные иконки через Image Asset Studio (Решение 2). Временное использование системных иконок (Решение 1) подходит для разработки.

#### Приложение не устанавливается

**Проверка**:
```powershell
# Проверьте, что устройство подключено
adb devices

# Попробуйте установить вручную
.\gradlew.bat :app:installDebug
```

**Решение**:
- Убедитесь, что на устройстве включена "Установка из неизвестных источников"
- Для эмулятора: обычно не требуется
- Для реального устройства: Settings → Security → Unknown sources (включить)

#### Приложение крашится при запуске

**Просмотр логов**:
```powershell
# Просмотр логов в реальном времени
adb logcat

# Фильтр по имени приложения
adb logcat | Select-String "EastclinicApp"
```

**В Android Studio**:
1. Запустите приложение в режиме отладки
2. Панель "Logcat" внизу покажет ошибки
3. Ищите строки с "FATAL EXCEPTION" или "AndroidRuntime"

### 6.4 Полезные команды для отладки

```powershell
# Очистка проекта
.\gradlew.bat clean

# Пересборка проекта
.\gradlew.bat clean build

# Остановка Gradle daemon (полезно при проблемах с кэшем)
.\gradlew.bat --stop

# Очистка build папок JVM модулей (если классы скомпилированы с неправильной версией Java)
Remove-Item -Recurse -Force "core\common\build" -ErrorAction SilentlyContinue
Get-ChildItem -Path "feature" -Directory -Recurse -Filter "domain" | ForEach-Object { Remove-Item -Recurse -Force "$($_.FullName)\build" -ErrorAction SilentlyContinue }

# Очистка кэша Configuration Cache (если включен)
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\caches\8.10\configuration-cache" -ErrorAction SilentlyContinue

# Очистка кэша трансформаций JdkImage (при ошибках jlink.exe)
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\caches\8.10\transforms\*" -ErrorAction SilentlyContinue

# Полная очистка кэша Gradle (осторожно: удалит все кэшированные зависимости)
Remove-Item -Recurse -Force "$env:USERPROFILE\.gradle\caches" -ErrorAction SilentlyContinue

# Просмотр зависимостей модуля
.\gradlew.bat :app:dependencies > dependencies.txt

# Проверка версии Gradle
.\gradlew.bat --version

# Просмотр всех доступных задач
.\gradlew.bat tasks
```

---

## 7. Скриншоты при ошибках

### 7.1 Автоматические скриншоты в UI тестах

**Compose Testing автоматически делает скриншоты** при падении теста, если настроено правильно.

**Настройка** (уже должно быть в проекте):
```kotlin
@get:Rule
val composeTestRule = createComposeRule()

@Test
fun testWithScreenshot() {
    composeTestRule.setContent {
        MyScreen()
    }
    
    // Если тест упадет здесь, скриншот будет автоматически сохранен
    composeTestRule.onNodeWithTag("button").performClick()
}
```

**Где найти скриншоты**:
```
app/build/outputs/androidTest-results/connected/screenshots/
```

### 7.2 Ручные скриншоты

**Через adb**:
```powershell
# Сделать скриншот экрана устройства
adb shell screencap -p /sdcard/screenshot.png

# Скачать скриншот на компьютер
adb pull /sdcard/screenshot.png screenshot.png
```

**Через Android Studio**:
1. Запустите приложение в режиме отладки
2. View → Tool Windows → Logcat
3. В панели устройств (Device Manager) нажмите кнопку камеры (📷) для скриншота

### 7.3 Скриншоты при ошибках тестов

**В Android Studio**:
1. Запустите UI тест
2. Если тест упал, в панели "Run" будет ссылка на скриншот
3. Кликните на ссылку, чтобы открыть скриншот

**В отчете тестов**:
- Откройте `app/build/reports/androidTests/connected/index.html`
- Для каждого упавшего теста будет ссылка на скриншот

### 7.4 Видео записи тестов (опционально)

**Запись видео выполнения теста**:
```powershell
# Начать запись
adb shell screenrecord /sdcard/test-video.mp4

# Выполнить тест
.\gradlew.bat :app:connectedAndroidTest

# Остановить запись (Ctrl+C в другом терминале)
# Скачать видео
adb pull /sdcard/test-video.mp4 test-video.mp4
```

---

## 8. Часто задаваемые вопросы

### 8.1 MCP сервер к Android Studio

**Вопрос**: Можно ли использовать MCP (Model Context Protocol) сервер для интеграции с Android Studio?

**Ответ**: 
- На данный момент прямого MCP сервера для Android Studio нет
- Android Studio имеет собственный API для плагинов, но это требует разработки плагина
- Для автоматизации можно использовать:
  - **Gradle команды** через командную строку (см. разделы выше)
  - **Android Studio Scripting** (ограниченные возможности)
  - **ADB команды** для управления устройствами

**Рекомендация**: Используйте Gradle команды и скрипты PowerShell для автоматизации тестирования.

### 8.2 Как часто нужно запускать тесты?

**Рекомендации**:
- **Unit тесты**: После каждого изменения кода (можно настроить автоматический запуск)
- **UI тесты**: Перед коммитом изменений или перед релизом
- **Все тесты**: В CI/CD пайплайне автоматически

### 8.3 Тесты работают медленно

**Оптимизация**:
- Запускайте только нужные тесты (конкретный модуль или класс)
- Используйте `--parallel` для параллельного запуска:
  ```powershell
  .\gradlew.bat test --parallel
  ```
- Для UI тестов: используйте быстрый эмулятор (не старые версии Android)

### 8.4 Как добавить новый тест?

**Unit тест**:
1. Создайте файл в `src/test/java/...` (в соответствующем модуле)
2. Название файла: `*Test.kt` (например, `UserRepositoryTest.kt`)
3. Используйте аннотацию `@Test`:
   ```kotlin
   import org.junit.Test
   
   class UserRepositoryTest {
       @Test
       fun `test user repository`() {
           // ваш тест
       }
   }
   ```

**UI тест**:
1. Создайте файл в `app/src/androidTest/java/...`
2. Используйте `createComposeRule()`:
   ```kotlin
   import androidx.compose.ui.test.junit4.createComposeRule
   
   @get:Rule
   val composeTestRule = createComposeRule()
   
   @Test
   fun testScreen() {
       composeTestRule.setContent {
           MyScreen()
       }
       // проверки
   }
   ```

### 8.5 Что делать, если тесты падают на CI, но работают локально?

**Возможные причины**:
- Разные версии JDK/Gradle
- Разные версии Android SDK
- Проблемы с кешем

**Решение**:
- Убедитесь, что версии в CI совпадают с локальными
- Очистите кеш в CI перед запуском тестов
- Проверьте логи CI для деталей ошибок

---

## 9. Быстрая справка команд

### Основные команды

```powershell
# Сборка проекта
.\gradlew.bat build

# Очистка проекта
.\gradlew.bat clean

# Запуск всех unit тестов
.\gradlew.bat test

# Запуск UI тестов (требует устройство)
.\gradlew.bat :app:connectedAndroidTest

# Установка приложения на устройство
.\gradlew.bat :app:installDebug

# Проверка подключенных устройств
adb devices

# Просмотр логов
adb logcat
```

### Тесты конкретных модулей

```powershell
# Тесты core:common
.\gradlew.bat :core:common:test

# Тесты feature:auth:presentation
.\gradlew.bat :feature:auth:presentation:test

# Тесты app модуля
.\gradlew.bat :app:test
```

### Отладка

```powershell
# Просмотр зависимостей
.\gradlew.bat :app:dependencies > dependencies.txt

# Версия Gradle
.\gradlew.bat --version

# Все доступные задачи
.\gradlew.bat tasks
```

---

## 10. Следующие шаги

После того, как вы успешно запустили тесты:

1. **Изучите существующие тесты** в проекте
2. **Напишите свои тесты** для нового функционала
3. **Настройте CI/CD** для автоматического запуска тестов
4. **Изучите паттерны тестирования** из [TESTING-STRATEGY.md](./TESTING-STRATEGY.md)

---

## Полезные ссылки

- [Официальная документация Android Testing](https://developer.android.com/training/testing)
- [Compose Testing документация](https://developer.android.com/jetpack/compose/testing)
- [JUnit 4 документация](https://junit.org/junit4/)
- [MockK документация](https://mockk.io/)
- [Gradle документация](https://docs.gradle.org/)

---

**Удачи в тестировании! 🚀**
