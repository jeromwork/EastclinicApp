package com.eastclinic.core.ui

/**
 * Marker interface for one-time UI events (navigation, messages).
 * Feature modules can create their own sealed classes implementing this interface.
 */
interface UiEffect
